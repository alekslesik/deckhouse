apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: digit-svd-ksh2-data-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: localpath
  resources:
    requests:
      storage: 1Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: digit-svd-ksh2-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digit-svd
      component: ksh2
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: digit-svd
        component: ksh2
    spec:
      serviceAccountName: digit-svd-sa
      imagePullSecrets:
        - name: digit-dgepu-secret
      containers:
      - name: ksh2
        namespace: digit
        image: 85.141.97.224:443/svd_ksh2:4.8.2.3k
        ports:
        - containerPort: 10132
        env:
        - name: SVDCode
          value: "ksh2"
        - name: SVDControl
          value: "digit-svd-control-service:11000"
        - name: TZ
          value: "Europe/Moscow"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
          - name: digit-svd-ksh2-data-rsc
            mountPath: /svd/host
      volumes:
      - name: digit-svd-ksh2-data-rsc
        persistentVolumeClaim:
          claimName: digit-svd-ksh2-data-pvc
