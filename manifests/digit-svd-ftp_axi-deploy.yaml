apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: digit-svd-ftp_axi-data-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: localpath
  resources:
    requests:
      storage: 1Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: digit-svd-ftp_axi-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digit-svd
      component: ftp_axi
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: digit-svd
        component: ftp_axi
    spec:
      serviceAccountName: digit-svd-sa
      imagePullSecrets:
        - name: digit-dgepu-secret
      containers:
      - name: ftp_axi
        namespace: digit
        image: 85.141.97.224:443/svd_ftp_axi:4.8.2.1k
        ports:
        - containerPort: 10122
        env:
        - name: SVDCode
          value: "ftp_axi"
        - name: SVDControl
          value: "digit-svd-control-service:11000"
        - name: TZ
          value: "Europe/Moscow"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
          - name: digit-svd-ftp_axi-data-rsc
            mountPath: /svd/host
      volumes:
      - name: digit-svd-ftp_axi-data-rsc
        persistentVolumeClaim:
          claimName: digit-svd-ftp_axi-data-pvc
