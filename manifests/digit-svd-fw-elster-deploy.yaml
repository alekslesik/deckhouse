apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: digit-svd-fw-elster-data-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: localpath
  resources:
    requests:
      storage: 1Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: digit-svd-fw-elster-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digit-svd
      component: fw-elster
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: digit-svd
        component: fw-elster
    spec:
      serviceAccountName: digit-svd-sa
      imagePullSecrets:
        - name: digit-dgepu-secret
      containers:
      - name: fw-elster
        namespace: digit
        image: 85.141.97.224:443/svd_fw-elster:4.8.2.0k
        ports:
        - containerPort: 10124
        env:
        - name: SVDCode
          value: "fw_elster"
        - name: SVDControl
          value: "digit-svd-control-service:11000"
        - name: TZ
          value: "Europe/Moscow"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
          - name: digit-svd-fw-elster-data-rsc
            mountPath: /svd/host
      volumes:
      - name: digit-svd-fw-elster-data-rsc
        persistentVolumeClaim:
          claimName: digit-svd-fw-elster-data-pvc
