apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: digit-svd-fw_axitech-data-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: localpath
  resources:
    requests:
      storage: 1Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: digit-svd-fw_axitech-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digit-svd
      component: fw_axitech
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: digit-svd
        component: fw_axitech
    spec:
      serviceAccountName: digit-svd-sa
      imagePullSecrets:
        - name: digit-dgepu-secret
      containers:
      - name: fw_axitech
        namespace: digit
        image: 85.141.97.224:443/svd_fw_axitech:4.8.2.1k
        ports:
        - containerPort: 10123
        env:
        - name: SVDCode
          value: "fw_axitech"
        - name: SVDControl
          value: "digit-svd-control-service:11000"
        - name: TZ
          value: "Europe/Moscow"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
          - name: digit-svd-fw_axitech-data-rsc
            mountPath: /svd/host
      volumes:
      - name: digit-svd-fw_axitech-data-rsc
        persistentVolumeClaim:
          claimName: digit-svd-fw_axitech-data-pvc
