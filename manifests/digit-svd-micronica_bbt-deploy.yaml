apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: digit-svd-micronica_bbt-data-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: localpath
  resources:
    requests:
      storage: 1Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: digit-svd-micronica_bbt-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digit-svd
      component: micronica_bbt
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: digit-svd
        component: micronica_bbt
    spec:
      serviceAccountName: digit-svd-sa
      imagePullSecrets:
        - name: digit-dgepu-secret
      containers:
      - name: micronica_bbt
        namespace: digit
        image: 85.141.97.224:443/svd_micronica_bbt:4.8.1.0.1k
        ports:
        - containerPort: 10135
        env:
        - name: SVDCode
          value: "micronica_bbt"
        - name: SVDControl
          value: "digit-svd-control-service:11000"
        - name: TZ
          value: "Europe/Moscow"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        volumeMounts:
          - name: digit-svd-micronica_bbt-data-rsc
            mountPath: /svd/host
      volumes:
      - name: digit-svd-micronica_bbt-data-rsc
        persistentVolumeClaim:
          claimName: digit-svd-micronica_bbt-data-pvc
